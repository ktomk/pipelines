# this .env file is for testing and is loaded *after* .env.dist
DOCKER_ID_USER=l-oracle-de-delphi
DOCKER_ID_PASSWORD=ThePastIsTheSecretOfTheFuture

# set directly (overwrite set in .env.dist)
FOO=BAZ
